"""Exercício 3 — Disparate impact e a regra dos 80%.

Setup: `taxa_a = 0.50` e `taxa_b = 0.45` (taxas de seleção dos grupos A e B).

Tarefa:
    Calcule a razão de impacto = (menor taxa) / (maior taxa). Aplique a regra
    dos 80%: passa se a razão for >= 0.8, senão reprova. Imprima
    `"razao de impacto: {ratio:.2f}"` e `"regra dos 80%: {passa|reprova}"`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/091-vieses-fairness/solucao_3.saida.txt
"""

# TODO: calcule a razão de impacto e aplique a regra dos 80%.
