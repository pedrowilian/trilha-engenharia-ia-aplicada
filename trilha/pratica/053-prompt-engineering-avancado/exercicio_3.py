"""Exercício 3 — Decomposição de tarefa em subtarefas.

Setup: a `frase` abaixo.

Tarefa:
    Implemente três subtarefas: `n_palavras` (contagem de palavras),
    `maiusculas` (texto em caixa alta) e `inverter_ordem` (palavras na ordem
    inversa, juntas por espaço). Monte um dict `sub` com chaves "n_palavras",
    "maiusculas" e "invertida" e imprima, nessa ordem, `chave: valor`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/053-prompt-engineering-avancado/solucao_3.saida.txt
"""

frase = "engenharia de ia aplicada"

# TODO: implementar as subtarefas, montar o dict e imprimir na ordem dada.
