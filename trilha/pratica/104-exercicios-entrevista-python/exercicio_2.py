"""Exercicio 2 - Two-sum em O(n) com tabela hash.

Setup (dado):
    nums = [1, 4, 5, 8, 12]
    consultas de alvo: 9, 20, 2.

Tarefa:
    Implemente two_sum(nums, alvo): percorra os numeros guardando {valor: indice}
    e, ao ver x cujo complemento (alvo - x) ja foi visto, devolva (indice_do_comp, i).
    Se nao houver par, devolva None. Imprima o resultado de cada consulta.

Criterio de conclusao (binario): a saida deve ser identica a
    trilha/solucoes/104-exercicios-entrevista-python/solucao_2.saida.txt
"""

nums = [1, 4, 5, 8, 12]

# TODO: implemente two_sum(nums, alvo) e imprima two_sum para alvo em 9, 20, 2.
