"""Exercício 2 — Ciclo gerar → criticar → revisar.

Setup: partes obrigatórias = ["abertura", "dados", "conclusao"]; rascunho
inicial = "abertura".

Tarefa:
    Implemente `criticar(texto)` (lista das partes faltantes) e
    `revisar(texto, falta)` (concatena a primeira parte faltante). Itere até 5
    vezes: a cada iteração imprima `iter {it}: falta={falta}`; se nada falta,
    imprima `aceito` e pare. Ao final imprima `final: {rascunho}`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/065-reflection/solucao_2.saida.txt
"""

# TODO: implemente o ciclo de reflexão.
