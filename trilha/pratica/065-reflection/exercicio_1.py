"""Exercício 1 — Crítico determinístico.

Setup: rascunhos a avaliar = ["titulo e mais texto", "curto"].

Tarefa:
    Implemente `criticar(texto)` que acumula problemas: "sem titulo" (se a
    palavra "titulo" não aparece) e "poucas palavras" (se há menos de 3
    palavras). A nota é `10 - 4 * len(problemas)`. Para cada rascunho, imprima
    `{texto!r}: nota={nota} problemas={problemas}`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/065-reflection/solucao_1.saida.txt
"""

rascunhos = ["titulo e mais texto", "curto"]

# TODO: implemente criticar e avalie cada rascunho.
