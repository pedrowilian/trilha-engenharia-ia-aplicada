"""Exercício 3 — Critério de aceitação.

Setup: `avaliar(versao) = 3 + 3 * versao`; `limiar = 9`; `max_revisoes = 5`.

Tarefa:
    Itere sobre as versões. A cada iteração imprima `versao {versao}: nota={nota}`.
    Se `nota >= limiar`, imprima `aceito por qualidade` e pare. Se
    `versao >= max_revisoes`, imprima `parou no limite de revisoes` e pare.
    Caso contrário, avance para a próxima versão.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/065-reflection/solucao_3.saida.txt
"""

# TODO: implemente o critério de aceitação por nota e por limite.
