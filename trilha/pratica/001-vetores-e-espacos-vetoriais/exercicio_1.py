"""Exercício 1 — Operações com vetores implementadas do zero (sem numpy).

Setup:
    u = [1.0, 2.0, 3.0]
    v = [4.0, 5.0, 6.0]

Tarefa:
    1. Implemente `soma(u, v)`: soma componente a componente.
    2. Implemente `escala(c, u)`: multiplica cada componente pelo escalar c.
    3. Imprima, exatamente neste formato:
        u + v   = [5.0, 7.0, 9.0]
        2 * u   = [2.0, 4.0, 6.0]
        u + 2*v = [9.0, 12.0, 15.0]

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/001-vetores-e-espacos-vetoriais/solucao_1.saida.txt
"""


def soma(u, v):
    # TODO: retornar a soma componente a componente de u e v
    raise NotImplementedError


def escala(c, u):
    # TODO: retornar o vetor u multiplicado pelo escalar c
    raise NotImplementedError


u = [1.0, 2.0, 3.0]
v = [4.0, 5.0, 6.0]

# TODO: imprimir u + v, 2 * u e u + 2*v usando as funções acima.
