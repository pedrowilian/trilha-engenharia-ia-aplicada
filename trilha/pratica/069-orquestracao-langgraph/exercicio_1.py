"""Exercício 1 — Estado compartilhado e nó.

Setup: estado inicial = {"valor": 3, "log": []}.

Tarefa:
    Implemente `no_dobrar(estado)` que devolve um NOVO estado (sem mutar o
    original) com `valor` dobrado e `"dobrou"` acrescido ao `log`. Aplique o nó
    duas vezes e imprima `valor: {valor}` e `log: {log}`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/069-orquestracao-langgraph/solucao_1.saida.txt (valor: 12).
"""

estado = {"valor": 3, "log": []}

# TODO: implemente no_dobrar e aplique-o duas vezes.
