"""Lição 008 — Exercício 1: Probabilidade clássica com dois dados.

Objetivo: calcular P(soma == 7) ao lançar dois dados honestos de 6 faces.

Passos:
  1. Construa o espaço amostral com todos os pares ordenados (a, b),
     com a, b em {1, ..., 6}.
  2. Conte os casos favoráveis em que a + b == 7.
  3. Calcule P = favoráveis / total.
  4. Imprima, com 4 casas decimais onde aplicável:
       |Omega|              = <total>
       favoraveis (soma=7)  = <n_favoraveis>
       P(soma=7)            = <probabilidade>

Critério de conclusão (binário): a saída deve ser EXATAMENTE
       |Omega|              = 36
       favoraveis (soma=7)  = 6
       P(soma=7)            = 0.1667
"""


def main() -> None:
    # TODO: implemente os 4 passos acima.
    raise NotImplementedError


if __name__ == "__main__":
    main()
