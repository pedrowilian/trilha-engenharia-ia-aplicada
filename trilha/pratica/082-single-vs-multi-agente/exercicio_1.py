"""Exercício 1 — Custo de coordenação.

Setup:
    tokens_por_subtarefa = 80
    overhead_handoff = 30
    subtarefas = [2, 4, 6]

Tarefa:
    Implemente `custo_single(n, t)` e `custo_multi(n, t, h)` e, para cada n,
    imprima `subtarefas={n}: single={s} multi={m} overhead={m - s}`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/082-single-vs-multi-agente/solucao_1.saida.txt
"""

tokens_por_subtarefa = 80
overhead_handoff = 30

# TODO: implemente os dois custos e imprima a comparação para n em [2, 4, 6].
