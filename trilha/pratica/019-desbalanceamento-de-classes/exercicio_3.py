"""Licao 019 — Exercicio 3: ajuste de limiar para maximizar F1.

Tarefa:
- Use np.random.default_rng(5), N=3000, 8% positivos. Scores: positivos
  ~ N(0.60, 0.15), negativos ~ N(0.35, 0.15), clipados em [0,1].
- Busque o limiar em linspace(0.3, 0.8, 26) que maximiza o F1.
- Imprima `F1 com limiar 0.5: ...`, `melhor limiar: <2 casas>`, `melhor F1: ...`
  (4 casas) e `limiar ajustado supera 0.5: <bool>`.

Criterio binario: saida IDENTICA a
trilha/solucoes/019-desbalanceamento-de-classes/solucao_3.saida.txt
"""
import numpy as np


def main():
    raise NotImplementedError


if __name__ == "__main__":
    main()
