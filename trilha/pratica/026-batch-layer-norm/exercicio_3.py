"""Licao 026 — Exercicio 3: eixos de Batch Norm vs Layer Norm.

Tarefa:
- Use X = [[1,2,6],[4,4,4]].
- Implemente batch_norm (normaliza por COLUNA, eixo 0) e layer_norm (normaliza
  por LINHA, eixo 1), ambos com eps=1e-5.
- Imprima `BN -> media por COLUNA:` (deve ser ~0 em cada coluna) e
  `LN -> media por LINHA:` (deve ser ~0 em cada linha).

Criterio binario: saida IDENTICA a
trilha/solucoes/026-batch-layer-norm/solucao_3.saida.txt
"""
import numpy as np


def main():
    raise NotImplementedError


if __name__ == "__main__":
    main()
