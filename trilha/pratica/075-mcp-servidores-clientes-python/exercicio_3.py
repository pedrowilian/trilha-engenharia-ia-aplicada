"""Exercício 3 — Ciclo tools/list + tools/call.

Setup: um servidor com as tools `subtrair` (a - b) e `dividir` (a / b),
    atendendo aos métodos `tools/list` e `tools/call`.

Tarefa:
    Construa o servidor (um handler que ramifica pelo `method`). Chame
    `tools/list` e imprima `tools: {lista ordenada}`. Depois chame `tools/call`
    para `subtrair` com {"a": 10, "b": 4} e imprima `subtrair(10,4) = {valor}`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/075-mcp-servidores-clientes-python/solucao_3.saida.txt
    (em particular, `subtrair(10,4) = 6`).
"""
# TODO: construa o servidor (tools/list + tools/call) e execute o ciclo.
