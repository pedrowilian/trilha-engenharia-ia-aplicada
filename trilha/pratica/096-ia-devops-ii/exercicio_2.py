"""Exercício 2 — FinOps: previsão de custo por ajuste linear.

Setup: meses = [1, 2, 3, 4, 5], custo = [200, 230, 260, 290, 320].

Tarefa:
    Implemente `ajuste_linear(xs, ys)` por mínimos quadrados (devolve
    inclinação e intercepto) e preveja o custo do mês 6. Imprima
    `inclinacao: {a:.2f} USD/mes` e `previsao mes 6: {valor:.2f} USD`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/096-ia-devops-ii/solucao_2.saida.txt.
"""

meses = [1, 2, 3, 4, 5]
custo = [200, 230, 260, 290, 320]

# TODO: implemente ajuste_linear e imprima a inclinacao e a previsao do mes 6.
