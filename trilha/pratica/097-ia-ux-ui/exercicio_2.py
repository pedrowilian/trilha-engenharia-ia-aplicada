"""Exercício 2 — Prototipação assistida: variantes de layout pontuadas.

Setup: n_componentes = 4.

Tarefa:
    Implemente `variantes(n_componentes)` que, para colunas em (1, 2, 3),
    calcula linhas = teto(n/colunas) e score = 10 - |colunas - 2|*2 -
    max(0, linhas - 3). Imprima `colunas=.. linhas=.. score=..` para cada
    variante.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/097-ia-ux-ui/solucao_2.saida.txt.
"""

n_componentes = 4

# TODO: implemente variantes e imprima cada variante.
