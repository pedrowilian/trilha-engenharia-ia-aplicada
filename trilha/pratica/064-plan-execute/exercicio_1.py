"""Exercício 1 — Planner estático.

Setup: a função `planejar(objetivo)` mapeia objetivos conhecidos para um plano
ordenado. Use o objetivo "deploy".

Tarefa:
    Implemente `planejar(objetivo)` com o mapa
    {"relatorio": ["coletar_dados", "analisar", "escrever", "revisar"],
     "deploy": ["testar", "build", "publicar"]} (objetivo desconhecido -> []).
    Para o objetivo "deploy", imprima cada passo numerado (`{i}. {passo}`) e,
    ao final, `total de passos: {n}`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/064-plan-execute/solucao_1.saida.txt
"""

# TODO: implemente planejar e imprima o plano de "deploy".
