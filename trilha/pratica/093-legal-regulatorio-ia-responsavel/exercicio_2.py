"""Exercício 2 — Checklist de conformidade.

Setup: o dicionário `requisitos` com cinco itens booleanos (no esqueleto):
    documentacao_tecnica=True, supervisao_humana=True, registro_de_logs=True,
    avaliacao_de_risco=True, transparencia_ao_usuario=False

Tarefa:
    Conte os requisitos atendidos, calcule `score = atendidos / total` e o status
    ("conforme" se `score >= 0.8`, senão "pendente"). Imprima
    `"requisitos atendidos: {a}/{t}"`, `"conformidade: {score:.0%}"` e
    `"status: {conforme|pendente}"`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/093-legal-regulatorio-ia-responsavel/solucao_2.saida.txt
"""

# TODO: calcule a fração de requisitos atendidos e o status.
