"""Exercício 1 — Economia de conectores M×N.

Setup: M = 4 aplicações e N = 6 fontes.

Tarefa:
    Calcule `sem_padrao = M * N` (conectores sob medida) e `com_mcp = M + N`
    (com um protocolo padrão). Imprima `sem padrao: {sem_padrao}`,
    `com mcp: {com_mcp}` e `reducao: {sem_padrao - com_mcp}`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/072-mcp-fundamentos/solucao_1.saida.txt
"""
M = 4
N = 6

# TODO: calcule sem_padrao e com_mcp e imprima as três linhas.
