"""Exercício 3 — Negociar capacidades por interseção.

Setup:
    cap_cliente = {"resources", "tools", "roots"}
    cap_servidor = {"tools", "prompts", "resources"}

Tarefa:
    Calcule `negociadas = cap_cliente & cap_servidor`. Imprima
    `negociadas: {lista ordenada}`, `usa resources? {True/False}` e
    `usa roots? {True/False}`.

Critério de conclusão (binário): a saída deve ser idêntica a
    trilha/solucoes/072-mcp-fundamentos/solucao_3.saida.txt
"""
cap_cliente = {"resources", "tools", "roots"}
cap_servidor = {"tools", "prompts", "resources"}

# TODO: calcule a interseção e imprima as três linhas.
