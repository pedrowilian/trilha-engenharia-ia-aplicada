"""Licao 030 — Exercicio 2: portao de atualizacao da GRU.

Tarefa:
- Implemente gru_update(z, h_prev, h_cand) = (1-z)*h_prev + z*h_cand.
- Com h_prev=1.0 e h_cand=0.0, para z em [0.0, 0.25, 0.5, 1.0], calcule h.
- Imprima por linha `z=...: h=...` (2 casas em z, 4 casas em h).

Criterio binario: saida IDENTICA a
trilha/solucoes/030-rnn-lstm-gru/solucao_2.saida.txt
"""


def gru_update(z, h_prev, h_cand):
    raise NotImplementedError


def main():
    raise NotImplementedError


if __name__ == "__main__":
    main()
